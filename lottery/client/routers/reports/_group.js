lotteryRoutes = FlowRouter.group({
    prefix: '/lottery'
    //subscriptions: function (params, queryParams) {
    //    // Global subscriptions for route group
    //    this.register('lottery_address', Meteor.subscribe('lottery_address'));
    //}
});
